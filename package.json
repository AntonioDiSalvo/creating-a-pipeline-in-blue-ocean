{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "jest-junit": "^13.1.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "test": "react-scripts test --env=jsdom --coverage --ci --testResultsProcessor=\"jest-junit\" --watchAll=false"
  },
  "jest": {
    "coverageReporters": [
      "text"
    ]
  }
}
